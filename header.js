import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

export default class Header extends React.Component {
  render(){ 
     return (
    <View style={{
        backgroundColor: 'rgb(51, 217, 188)',
        padding: 20,
        fontSize: 22,
        fontWeight: 'bold',
        textAlign: 'center',
        justifyContent:"center",
textAlign:"center",
marginTop:1,
marginBottom:50
    }}>
buzzApp
    </View>
  );
     }
}